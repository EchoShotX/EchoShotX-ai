# Core
boto3==1.35.30
botocore==1.35.30
requests==2.32.3
urllib3==2.2.3
python-dotenv==1.0.1

# Video processing
opencv-python==4.10.0.84
ffmpeg-python==0.2.0

# Logging & Utils
loguru==0.7.2

# Testing
pytest==8.3.3
pytest-mock==3.14.0
moto==5.0.11

# Type hints
typing-extensions==4.12.2

# Optional: for AWS S3/SQS mocking & development
boto3-stubs[s3,sqs]==1.35.30.0

# Config
pydantic-settings==2.2.1  # 정확한 버전 지정
# requests 이미 Core에 있음 → 제거
# cv2는 opencv-python으로 설치됨 → 제거

# REAL-ESRGAN & Deep Learning
# torch==2.2.1
# torchvision==0.17.1
# torchaudio==2.2.1
# realesrgan==0.3.0        # pip에서 설치 가능한 최신 버전
# basicsr==1.4.2           # RRDBNet 포함

# PyTorch + CUDA 12.1 (CUDA 12.x 드라이버용)
torch==2.4.1+cu121 --index-url https://download.pytorch.org/whl/cu121
torchvision==0.19.1+cu121 --index-url https://download.pytorch.org/whl/cu121
torchaudio==2.4.1+cu121 --index-url https://download.pytorch.org/whl/cu121

# Real-ESRGAN 관련
basicsr==1.4.2
facexlib==0.3.1
gfpgan==1.3.0
realesrgan==0.3.0