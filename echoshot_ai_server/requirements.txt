# Core - AWS & HTTP
boto3==1.35.30
botocore==1.35.30
requests==2.32.3
urllib3==2.2.3
python-dotenv==1.0.1

# Video processing
# opencv-python은 Dockerfile의 베이스 이미지에 CUDA 지원 버전이 포함되어 있으므로 제외
# ffmpeg-python은 ffmpeg 명령어 래퍼
ffmpeg-python==0.2.0

# Logging & Utils
loguru==0.7.2

# Config
pydantic-settings==2.2.1

# Redis (진행률 Pub/Sub용)
redis==5.0.1

# Type hints
typing-extensions==4.12.2

# NumPy (OpenCV 호환성)
# numpy>=1.26,<2.0   # Dockerfile에서 고정 설치하므로 제거

# Optional: for AWS S3/SQS mocking & development
boto3-stubs[s3,sqs]==1.35.30.0

# Testing
pytest==8.3.3
pytest-mock==3.14.0
moto==5.0.11
